{
  "buildCommand": "export PATH=$PATH:./node_modules/.bin && prisma generate && next build",
  "installCommand": "rm -rf node_modules && rm -f package-lock.json && npm install",
  "framework": "nextjs"
}
